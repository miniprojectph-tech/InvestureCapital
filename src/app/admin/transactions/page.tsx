import { PagePlaceholder } from "@/components/PagePlaceholder";
export default function Page() {
  return (
    <PagePlaceholder
      title="Platform transactions"
      subtitle="All money-in / money-out across every investor. Needs Firestore collectionGroup query — coming next."
    />
  );
}
