import { PagePlaceholder } from "@/components/PagePlaceholder";
export default function Page() {
  return (
    <PagePlaceholder
      title="Platform activity log"
      subtitle="Every event across every investor — payouts, compounds, activations. Needs Firestore collectionGroup query — coming next."
    />
  );
}
